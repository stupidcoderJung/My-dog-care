# 🕵️ Task 1: YOLOClient Implementation

## 🎯 Objective
Create a `YOLOClient` class that loads the `yolo11n.mlpackage` and performs object detection on `CVPixelBuffer`.

## 📋 Detailed Steps
1.  **Create File**: `ios-app/MyDogCare/Services/Vision/YOLOClient.swift`.
2.  **Load Model**: Use `try? VNCoreMLModel(for: yolo11n().model)`.
3.  **Request**: Create `VNCoreMLRequest`.
4.  **Inference**: Implement `predict(pixelBuffer: CVPixelBuffer) async throws -> [DetectedObject]`.
5.  **Post-Processing**:
    *   Filter by Confidence (> 0.5).
    *   Filter by Class (only 'dog' if possible, or map class IDs).
    *   Convert `VNRecognizedObjectObservation` to a custom struct `DetectedObject`.

## 🤖 AI Execution Prompt
(Copy this to the AI Agent)

```text
@ios-app/MyDogCare/Services/Vision/

I need you to execute **Task 1: YOLOClient Implementation**.

**Instructions**:
1.  Create a new file `ios-app/MyDogCare/Services/Vision/YOLOClient.swift`.
2.  Define a struct `DetectedObject`:
    ```swift
    struct DetectedObject: Identifiable {
        let id = UUID()
        let label: String
        let confidence: Float
        let boundingBox: CGRect // Normalized [0, 1]
    }
    ```
3.  Implement `class YOLOClient`:
    *   **Properties**: `private var request: VNCoreMLRequest?`
    *   **Init**: Load `yolo11n` (assume the class is generated by Xcode, but if not, load by resource name "yolo11n").
        *   *Note*: Since we manually added the model, we might need to load it via `MLModel(contentsOf: url)`. Let's try the generated class `yolo11n()` first. If that fails, use resource URL.
    *   **Method**: `func predict(pixelBuffer: CVPixelBuffer) async throws -> [DetectedObject]`
        *   Create `VNImageRequestHandler`.
        *   Perform request.
        *   Map results to `[DetectedObject]`.
        *   **Filter**: Only return objects with confidence > 0.5.

**Deliverable**:
*   `YOLOClient.swift` file with robust error handling.
```
